/*
[难度: ★]
[所属知识点: 三问框架认知]
[预计完成时间: 5 分钟]

题目:三问决策框架的三个问题,正确的顺序是?
    A. 需要重新赋值? → 需要在块外访问? → 是逻辑常量?
    B. 是逻辑常量? → 需要重新赋值? → 需要在块外访问?
    C. 需要在块外访问? → 需要重新赋值? → 是逻辑常量?

给定场景:循环计数器 → 应该选 let 还是 const?
*/

if (require.main === module) {
  console.log("答案:A");
  console.log("");
  console.log("循环计数器 → let");
  console.log("  理由:Q1:需要重新赋值? 是 → 进入 Q2");
  console.log("       Q2:需要在块外访问? 否 → let(块级)");
}
