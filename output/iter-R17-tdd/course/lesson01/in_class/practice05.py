"""
[难度: ★★★][所属知识点: 综合][预计完成时间: 15 分钟]

题目:下面代码是"伪 TDD"(先写代码后补测试),识别问题。

# 先写了代码
def add(a, b):
    return a + b

# 后补的测试(测的是实现,不是行为)
def test_add():
    assert add(2, 3) == 5  # 这个测试没问题
    assert add.__code__.co_code == b'...'  # 测了实现细节!

问题:test_add 的第二行测了实现细节(字节码),不是行为。
修正:只测输入→输出,不测内部实现。
"""
