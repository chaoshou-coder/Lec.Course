"""
[难度: ★★★★][所属知识点: Pandas 数据分析综合][预计完成时间: 25 分钟]

题目:创建学生成绩 DataFrame:
    姓名  数学  语文  英语
   张三   85    78    92
   李四   90    88    76
   王五   72    95    83
   赵六   88    82    90

1. 新增一列 "总分",等于三科之和
2. 新增一列 "平均分",等于三科均值(保留 1 位小数)
3. 用 loc 筛选出平均分 >= 85 的学生
4. 按总分降序排序(用 sort_values),打印排序后的姓名和总分

示例:
    平均分 >= 85 的学生:
    张三, 李四, 赵六
    排序后:
    赵六 260
    张三 255
    李四 254
"""

import pandas as pd

# TODO: 创建 DataFrame
# TODO: 新增"总分"和"平均分"列
# TODO: 筛选平均分 >= 85 并按总分降序排列
pass

if __name__ == '__main__':
    # 参考答案
    data = {"姓名": ["张三", "李四", "王五", "赵六"],
            "数学": [85, 90, 72, 88],
            "语文": [78, 88, 95, 82],
            "英语": [92, 76, 83, 90]}
    df = pd.DataFrame(data)
    df["总分"] = df["数学"] + df["语文"] + df["英语"]
    df["平均分"] = (df["总分"] / 3).round(1)
    high = df.loc[df["平均分"] >= 85]
    print("平均分 >= 85 的学生:", list(high["姓名"]))
    ranked = df.sort_values("总分", ascending=False)
    print(ranked[["姓名", "总分"]])
