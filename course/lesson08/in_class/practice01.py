"""
[难度: ★★]
[所属知识点: 语义化标签辨析]
[预计完成时间: 10 分钟]

题目:下面哪些场景应该用语义化标签,哪些仍适合用 div?
在括号里填写应该使用的标签名(如果仍用 div 则写 div)。

    1. 页面顶部放 logo 和主导航       (  )
    2. 包裹一段装饰性背景色的容器     (  )
    3. 页面底部的版权与备案信息       (  )
    4. 一组"关于我们/服务/联系"链接  (  )
    5. 文章正文的最外层容器(唯一)     (  )
    6. 一个没有任何标题的纯图片轮播   (  )
    7. 一篇独立的博客文章             (  )
    8. 页面右侧边栏(广告/推荐阅读)    (  )

提示:语义标签 = 名字能说明用途;div = 纯容器,无语义。
"""

# 学员不需要写代码,在纸上作答即可。
# 参考答案见下方测试区。

if __name__ == '__main__':
    answers = {
        1: "<header>  — 页面顶部头部",
        2: "div       — 纯装饰容器,无语义含义",
        3: "<footer>  — 页面底部脚部",
        4: "<nav>     — 一组导航链接",
        5: "<main>    — 主体内容唯一容器",
        6: "div       — 无标题的纯视觉容器,用 div",
        7: "<article> — 独立可分发的文章",
        8: "<aside>   — 侧边栏附加内容",
    }
    for k, v in answers.items():
        print(f"  {k}. {v}")
